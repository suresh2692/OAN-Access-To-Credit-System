allowBuilds:
  '@parcel/watcher': false
  msw: false
  sharp: false
  unrs-resolver: false

overrides:
  postcss: "^8.5.18"
  brace-expansion: "^5.0.8"
  immutable: "^5.1.8"
  sharp: "^0.35.0"
  # minimatch@3 (an ESLint dependency) needs brace-expansion's old ^1.x API —
  # scoped so the blanket ^5.0.8 fix above still applies to everything else.
  "minimatch@3>brace-expansion": "^1.1.12"
